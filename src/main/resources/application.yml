spring:
  datasource:
    url: jdbc:mysql://viaduct.proxy.rlwy.net:20559/railway
    username: ${MYSQLUSER}
    password: ${MYSQL_ROOT_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update